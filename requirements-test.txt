# Minimal dependency set required to COLLECT and run the test suite.
# Deliberately small: the full curriculum environment (torch, tensorflow,
# prophet, geopandas, ...) is NOT needed for the gates. Heavy optional stacks
# are guarded with pytest.importorskip in the tests that reference them.
pytest>=8
nbformat>=5.10
numpy>=2
scipy>=1.18
pandas>=2.3
matplotlib>=3.11
