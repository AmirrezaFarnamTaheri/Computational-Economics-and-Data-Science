<!-- AUTO-GENERATED by scripts/notebooks_to_docs.py; edit the notebook source instead. -->

> **Source notebook:** [`08-Time-Series/05_Volatility_Modeling_ARCH_GARCH.ipynb`](https://github.com/AmirrezaFarnamTaheri/Computational-Economics-and-Data-Science/blob/main/08-Time-Series/05_Volatility_Modeling_ARCH_GARCH.ipynb)

> Notebook outputs are intentionally omitted from the documentation build; code and narrative remain source-faithful.

# 05 Volatility Modeling ARCH GARCH

[![Open in Colab](https://colab.research.google.com/assets/colab-badge.svg)](https://colab.research.google.com/github/AmirrezaFarnamTaheri/Computational-Economics-and-Data-Science/blob/main/08-Time-Series/05_Volatility_Modeling_ARCH_GARCH.ipynb) [![Launch Binder](https://mybinder.org/badge_logo.svg)](https://mybinder.org/v2/gh/AmirrezaFarnamTaheri/Computational-Economics-and-Data-Science/main?filepath=08-Time-Series/05_Volatility_Modeling_ARCH_GARCH.ipynb) [![Code License: MIT](https://img.shields.io/badge/Code%20License-MIT-yellow.svg)](../LICENSE) [![Content License: CC BY 4.0](https://img.shields.io/badge/Content%20License-CC%20BY%204.0-blue.svg)](https://creativecommons.org/licenses/by/4.0/)

```python
# === Environment Setup ===
import warnings

import matplotlib.pyplot as plt
import numpy as np
import pandas as pd
import scipy.stats as stats
from arch import arch_model

# --- Configuration ---
plt.style.use('seaborn-v0_8-whitegrid')
plt.rcParams.update({'font.size': 12, 'figure.figsize': (10, 6), 'figure.dpi': 130,
                     'axes.titlesize': 'x-large', 'axes.labelsize': 'large',
                     'xtick.labelsize': 'medium', 'ytick.labelsize': 'medium'})
%config InlineBackend.figure_format = 'retina'
np.set_printoptions(suppress=True, linewidth=120, precision=4)
```

# Table of Contents
* [The Lens: The Pulse of Fear](#the-lens-the-pulse-of-fear)
* [1. The Stylized Facts of Financial Returns](#1-the-stylized-facts-of-financial-returns)
  * [1.1 Visualizing Fat Tails](#11-visualizing-fat-tails)
* [2. The GARCH(1,1) Model](#2-the-garch11-model)
* [3. Asymmetry: The Leverage Effect](#3-asymmetry-the-leverage-effect)
* [4. Summary](#4-summary)

## The Lens: The Pulse of Fear
In classical economics, risk is often treated as a constant parameter. In financial markets, risk breathes. It spikes during crises, sleeps during booms, and clusters in time. If the market crashes today, it is highly likely to be volatile tomorrow. This phenomenon is called **Volatility Clustering**.

Standard regression models (like OLS) fail here because they assume constant variance (homoskedasticity). To price options, manage risk, or forecast value-at-risk (VaR), we need models that treat volatility as a dynamic process.

This notebook introduces the Nobel Prize-winning **ARCH** (Autoregressive Conditional Heteroskedasticity) and **GARCH** models.



**Economic question.** In *05 Volatility Modeling ARCH GARCH*, what must remain economically invariant when the computational representation changes? Time-series methods exploit dependence across dates, so chronology is part of the data-generating process rather than a nuisance index. The central question is what can be learned from persistence, innovations, and cross-variable dynamics without leaking future information into the past. Model adequacy must therefore be judged through residual diagnostics, stability, and genuinely out-of-sample forecasting or structural restrictions.

### Learning Objectives
* **Identify** the stylized facts of financial returns: volatility clustering, fat tails, and the leverage effect.
* **Estimate** GARCH(1,1) models and interpret the persistence parameters.
* **Extend** to GJR-GARCH to capture asymmetric volatility responses.
* **Apply** conditional volatility forecasts to risk management (Value-at-Risk).

### Prerequisites
* **ARMA/ARIMA:** Autoregressive modeling and residual diagnostics (Module 08 - ARMA/ARIMA).
* **Statistics:** Distributions, kurtosis, and Q-Q plots.
* **Python:** `arch` package, `statsmodels`, and Matplotlib.
* **Learning-path prerequisite:** [`04C_VAR_Impulse_Responses_and_FEVD.ipynb`](https://github.com/AmirrezaFarnamTaheri/Computational-Economics-and-Data-Science/blob/main/08-Time-Series/04C_VAR_Impulse_Responses_and_FEVD.ipynb)

> **Learning path:** Building on [`04C_VAR_Impulse_Responses_and_FEVD.ipynb`](https://github.com/AmirrezaFarnamTaheri/Computational-Economics-and-Data-Science/blob/main/08-Time-Series/04C_VAR_Impulse_Responses_and_FEVD.ipynb); next continue with [`06_Cointegration_and_Error_Correction_Models.ipynb`](https://github.com/AmirrezaFarnamTaheri/Computational-Economics-and-Data-Science/blob/main/08-Time-Series/06_Cointegration_and_Error_Correction_Models.ipynb).

## 1. The Stylized Facts of Financial Returns

Financial time series exhibit three key properties that break standard Gaussian assumptions:
1.  **Absence of Autocorrelation:** Returns are hard to predict ($E[r_{t+1}|r_t] \approx 0$).
2.  **Volatility Clustering:** Squared returns are highly autocorrelated. Large moves follow large moves.
3.  **Fat Tails (Leptokurtosis):** Extreme events (6-sigma moves) happen far more often than a Normal distribution allows.

```python
# Load Local Data
try:
    sp500 = pd.read_csv('../data/sp500.csv', index_col='Date', parse_dates=True)
    # Check if 'Close' or 'Adj Close' exists
    if 'Adj Close' in sp500.columns:
        prices = sp500['Adj Close']
    else:
        prices = sp500['Close']

    # Calculate Returns (Scale by 100 for numerical stability in optimizer)
    returns = 100 * prices.pct_change().dropna()
    print("> **Note:** Loaded S&P 500 data from local storage.")

except FileNotFoundError:
    print("Error: '../data/sp500.csv' not found. Generating synthetic GARCH process.")
    np.random.seed(42)
    n = 2000
    w = 0.05
    alpha = 0.1
    beta = 0.85
    y = np.zeros(n)
    sigma2 = np.zeros(n)
    sigma2[0] = w / (1 - alpha - beta)
    for t in range(1, n):
        sigma2[t] = w + alpha * y[t-1]**2 + beta * sigma2[t-1]
        y[t] = np.sqrt(sigma2[t]) * np.random.normal()

    returns = pd.Series(y, index=pd.date_range('2015-01-01', periods=n, freq='D'))
    returns.name = 'Synthetic Returns'

# Plot Returns
plt.figure(figsize=(12, 5))
returns.plot(alpha=0.6, linewidth=0.8)
plt.title('Daily Returns: Volatility Clustering in Action')
plt.ylabel('Percentage Return')
plt.show()
```

### 1.1 Visualizing Fat Tails
We compare the empirical distribution of returns against a Normal distribution with the same mean and variance. The "peaked" center and "heavy" tails are classic signs of non-normality.

```python
fig, (ax1, ax2) = plt.subplots(1, 2, figsize=(14, 5))

# Histogram
mu, std = stats.norm.fit(returns)
ax1.hist(returns, bins=80, density=True, alpha=0.6, color='steelblue', label='Actual Returns')
xmin, xmax = ax1.get_xlim()
x = np.linspace(xmin, xmax, 100)
p = stats.norm.pdf(x, mu, std)
ax1.plot(x, p, 'k', linewidth=2, label='Normal Dist')
ax1.set_title('Distribution of Returns vs. Normal')
ax1.legend()

# Q-Q Plot
stats.probplot(returns, dist="norm", plot=ax2)
ax2.set_title('Q-Q Plot (Normal Probability Plot)')
plt.show()
```

## 2. The GARCH(1,1) Model

The standard GARCH(1,1) specification is:

1.  **Mean Equation:** $r_t = \mu + \epsilon_t$
2.  **Variance Equation:** $\sigma_t^2 = \omega + \alpha \epsilon_{t-1}^2 + \beta \sigma_{t-1}^2$

Where:
- $\alpha$ captures the reaction to new shocks (ARCH term).
- $\beta$ captures the persistence of old volatility (GARCH term).
- Stationarity requires $\alpha + \beta < 1$.

```python
# Fit GARCH(1,1)
model = arch_model(returns, mean='Constant', vol='GARCH', p=1, q=1)
res = model.fit(disp='off')

print(res.summary())

# Extract Conditional Volatility
cond_vol = res.conditional_volatility

# Plot
plt.figure(figsize=(12, 5))
plt.plot(cond_vol, color='red', linewidth=1)
plt.title('Estimated Conditional Volatility (Annualized)')
plt.ylabel('Daily Volatility (%)')
plt.show()
```

## 3. Asymmetry: The Leverage Effect

Markets often crash faster than they recover. Bad news increases volatility more than good news. The standard GARCH model is symmetric (it squares the residuals). The **GJR-GARCH** model adds an interaction term to capture this:

$$ \sigma_t^2 = \omega + \alpha \epsilon_{t-1}^2 + \gamma I_{t-1}\epsilon_{t-1}^2 + \beta \sigma_{t-1}^2 $$

where $I_{t-1} = 1$ if $\epsilon_{t-1} < 0$. A positive $\gamma$ implies the leverage effect.

```python
# Fit GJR-GARCH(1,1)
# o=1 adds the asymmetry term
model_gjr = arch_model(returns, mean='Constant', vol='GARCH', p=1, o=1, q=1)
res_gjr = model_gjr.fit(disp='off')

print("> **Note:** GJR-GARCH Parameter Estimates:")
print(res_gjr.params)

gamma_val = res_gjr.params.get('gamma[1]', 0)
if gamma_val > 0:
    print(f"\n> **Observation:** Gamma is positive ({gamma_val:.4f}). This confirms the leverage effect: negative shocks increase volatility more than positive ones.")
else:
    print("\n> **Observation:** No significant leverage effect detected.")
```

## Exercises

**1. Mechanism and assumptions (Conceptual):** For **05 Volatility Modeling ARCH GARCH**, identify the stochastic assumptions that make the model estimable and state how stationarity, invertibility, or identification can be checked from the fitted object.

**2. Reproduce and diagnose (Applied):** Fit the method covered in 1. The Stylized Facts of Financial Returns, 1.1 Visualizing Fat Tails to a time-ordered series. Diagnose residual dependence and stability, then evaluate a rolling or expanding-window out-of-sample forecast against a naive baseline.

**3. Robust extension (Challenge):** Alter one structural restriction, lag/order choice, or innovation distribution. Explain how impulse responses, forecasts, or uncertainty change and whether the conclusion survives the alternative specification.

<details>
<summary>Solution guidance</summary>

A strong solution states assumptions before computation, includes an independent diagnostic or limiting-case check, and interprets the result in the units of the economic problem. For the challenge, separate changes caused by the economic assumption from changes caused by numerical approximation or tuning.

</details>

## 4. Summary

1.  **Volatility is Predictable:** Even if returns are random, risk is not. It clusters.
2.  **GARCH:** This model captures the memory of risk. High $\beta$ values (typically > 0.9) indicate that market fear dissipates slowly.
3.  **Risk Management:** By forecasting $\sigma_{t+1}$, we can construct better Value-at-Risk (VaR) models than by simply assuming a constant historical standard deviation.

## References & Further Reading

- Hamilton, J. D. (1994). *Time Series Analysis*. Princeton University Press.
- Lütkepohl, H. (2005). *New Introduction to Multiple Time Series Analysis*. Springer.
- Hyndman, R. J. & Athanasopoulos, G. *Forecasting: Principles and Practice* (3rd ed.). OTexts.
